package _1Intro

fun main(args: Array<String>) {
    val name = if (args.size > 0) args[0] else "Kotlin"   // it returns a value -  if is an expression
    println("Hello, $name!") // concatenation

    // ${}
}
